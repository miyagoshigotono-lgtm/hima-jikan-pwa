# 暇時間即答PWA 仕様書

## 1. 背景・目的

「俺って来月いつ暇？」という問いに、数秒〜数十秒で答えが返ってくる仕組みを作る。目的は日々の意思決定コストを減らすこと。

現状の課題：
- 予定がカレンダーに自然に溜まっていく入口がない
- 予定を参照して「暇な時間」を計算する処理がない

この2つを1つのPWAに実装する。

## 2. スコープ（今回実装する範囲）

- **入力側**：スマホで音声入力 → 自然言語をパース → Googleカレンダーに予定を追加
- **参照側**：スマホで音声（またはテキスト）入力 → 自然言語で質問 → Googleカレンダーを読み取り、後述の「暇時間帯判定ルール」に基づいて空き時間を計算 → 自然言語で回答

両方を今回のPWAに実装する。

## 3. 前提となる既存資産

- Googleカレンダー `miyago.shigotono@gmail.com` に、2026年分の「休み」終日予定が111件登録済み（会社の年間休日表を元に一括登録済み）
- ユーザー（宮後）は音声入力を使った自作アプリの開発経験あり
- ユーザーはGAS/Python/ESP32/PWA歴3年以上の自作ツール開発者

## 4. 暇時間帯の判定ルール（確定仕様）

これがこのアプリの中核ロジック。**単発休みと連休の区別なく、1本のルールで統一されている。**

> 休みブロック（カレンダー上で「休み」予定が1日以上連続している区間）の**前夜18:00**から、その休みブロック**最終日の22:00**まで、途中の区切り（睡眠時間帯など）を一切考慮せず、ノンストップで「暇時間帯」とする。

### 具体例

- 単発休み（例：土曜のみ休み、金曜・日曜は仕事）
  → 金曜18:00 〜 土曜22:00（28時間通し）
- 2連休（例：土日連続休み）
  → 金曜18:00 〜 日曜22:00（52時間通し）
- n連休
  → 休みに入る前日18:00 〜 休み最終日22:00（途中断絶なし）

### 補足

- 休みの判定は、カレンダー上の「休み」という終日予定の有無で行う
- 「休み」予定と重複する他の予定（別の用事等）がもしあれば、その時間帯は暇時間帯から除外する（＝暇時間帯 = 休みブロックの計算区間 − その区間に重なる他の予定）
- 睡眠時間帯（0:00〜7:00）を暇時間帯から除外する処理は**行わない**（今回のルールで無効化されたため）

## 5. システム構成

```
[スマホ音声入力(PWA)]
        │ 発話テキスト
        ▼
[GAS Webエンドポイント]
        │ 発話テキストを渡す
        ▼
[Gemini API]
        │ 意図判定 + 構造化データに変換
        │  - 意図: 「予定追加」 or 「空き時間照会」
        │  - 予定追加の場合: 日時・タイトル・場所・相手等を抽出
        │  - 空き時間照会の場合: 対象期間（今週/来月/8月後半等）を抽出
        ▼
[GAS] Googleカレンダー操作
        │
        ├─ 予定追加の場合: CalendarApp で予定を作成
        │
        └─ 空き時間照会の場合:
              1. 対象期間のイベント一覧を取得
              2. 「休み」予定から休みブロックを検出
              3. 上記ルールで暇時間帯を計算
              4. 他の予定との重複を除外
              5. 計算結果をGemini APIに渡し、自然言語の回答文を生成
        ▼
[結果をPWAに返却 → 音声 or テキストで表示]
```

## 6. 各コンポーネントの実装方針

### 6.1 フロントエンド（PWA）

- 音声入力はWeb Speech API等、ユーザーが過去に音声入力アプリを作った際の実装パターンを踏襲してよい
- 発話→テキスト変換後、GASのWebエンドポイントにPOST
- 結果はテキスト表示、可能であれば音声合成での読み上げも検討（必須ではない）
- オフライン時の挙動：ネットワーク必須の機能のため、オフライン時はその旨を明示すれば足りる（オフラインキャッシュは今回のスコープ外）

### 6.2 バックエンド（GAS）

- Webアプリとして公開し、PWAからのPOSTリクエストを受ける
- Gemini APIを呼び出し、発話テキストから意図と構造化データを抽出する
- Googleカレンダー操作は `CalendarApp` を使用
- 対象カレンダー：`miyago.shigotono@gmail.com`

### 6.3 Gemini API連携

- 用途：自然言語の意図分類＋情報抽出、および計算結果の自然言語化
- 無料枠を意識し、軽量モデル（Flash系）を優先する
- プロンプト設計は「予定追加」「空き時間照会」の2種類の意図を判別できるようにする

### 6.4 実装方式の裁量

以下はClaude Codeの判断に委ねる：

- 新規GASプロジェクトとして構築するか、GitHubリポジトリで管理しCLaspでデプロイするか
- コードの言語・フレームワーク構成（PWA側のフロントエンド技術選定含む）
- Gemini APIのプロンプト設計の詳細
- エラーハンドリングの詳細設計

## 7. 想定される利用例

**予定追加**
> ユーザー「来週の土曜19時から田中さんと居酒屋で飲む予定入れといて」
> → カレンダーに「田中さんと飲み会」等のタイトルで該当日時の予定が追加される

**空き時間照会**
> ユーザー「来月どこが空いてる？」
> → カレンダーの休みブロックを元に暇時間帯を計算し、「来月は8/1(土)18:00〜8/2(日)22:00、8/8(土)18:00〜8/9(日)22:00...が空いています」のように自然言語で回答

## 8. 非スコープ（今回は対応しない）

- 椅子張り創圭（副業）の予約管理（副業自体が未スタートのため）
- 睡眠時間帯を考慮した細かい時間帯除外（今回のルールでは不要と確定済み）
- LINE等、PWA以外の入力経路
- 会社の年間休日表の自動取り込み（今回は一括登録済みで完了、来年以降は都度対応）

## 9. 参考：関連ルール・注意点

- ユーザーの開発ワークフロー上、フェーズに応じたモデル選定の慣習がある（初期相談はHaiku、仕様詰め・実装はSonnet中エフォート〜Opus高エフォート等）。Claude Codeが自律開発する場合も、タスクの複雑度に応じてモデル・工数を意識するとよい
- ベストプラクティスや改善点は後出しせず、設計段階で全て提示する運用を好む
- 完成版を無許可で出さず、方針合意→許可→出力の順を踏む運用を好む
